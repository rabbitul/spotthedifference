# Spot the Difference – Project Notes

React + TypeScript + Vite. SVG scenes. Deploy: GitHub (`rabbitul/spotthedifference`) + Vercel (repo ID `1224386498`).

## Adding a real-photo level
Run `/add-photo-level` — the skill contains the full step-by-step workflow.
